"use client";

/**
 * @deprecated Use AppShell / AppSidebar / BottomTabBar instead.
 * Stub kept so old imports do not crash during the redesign.
 */
export default function Navbar() {
  return null;
}
